Check recent commit style with `git log --oneline -10`, then commit staged changes matching that style.

Rules:
- English, concise (one-line preferred)
- Never add Co-authored-by or Signed-off-by trailers
- Do not push
